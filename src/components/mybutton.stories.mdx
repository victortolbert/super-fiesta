import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import BaseButton from './BaseButton';

<Meta title="Base|Button" component={BaseButton} />

# MyButton

With `MDX` we can define a story for `MyButton` right in the middle of our
markdown documentation.

<Preview>
  <Story name="Mdx Story for BaseButton">
    {{
        components: { BaseButton },
        template: '<BaseButton @click="action">Hello Button</BaseButton>',
        methods: { action: action("clicked") }
    }}
  </Story>
</Preview>
